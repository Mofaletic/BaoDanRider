# Codex Tasks

## 下一批优先任务

1. 增加 DataAsset 类型：餐品、危险、升级、订单风险。
2. 给 `BDRRiderPawn` 接入 Enhanced Input。
3. 做最小 UMG：Hub、配送 HUD、结算界面。
4. 增加一键创建 `Hub` 和 `DeliveryCity` 地图的 Editor 工具。
5. 增加自动化测试：结算公式、升级购买、保温箱占格、存档读写。
6. 根据实际运行手感调整 `BDRRiderPawn` 的速度、刹车和转向。

## 每次迭代模板

目标：

改动范围：

测试方法：

验收标准：

风险：
